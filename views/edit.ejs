<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Chat</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link rel="stylesheet" href="/style.css"/>
</head>
<body class="bg-dark text-light">
  <div class="container mt-5">
    <h3 class="chat-heading mb-4">Edit Chat</h3>
    <form action="/chats/<%= chats._id %>?_method=PUT" method="POST">
      <div class="card shadow-sm bg-dark text-light border-secondary">
        <div class="card-body">
          <p class="text-end bi"><%= chats.created_at.toString().split(' ').slice(0,4).join("-") %></p>
          <p><i class="bi bi-person-fill"></i> From: <i><%= chats.from %></i></p>
          <p><i class="bi bi-person"></i> To: <i><%= chats.to %></i></p>
          <textarea name="message" class="form-control bg-dark text-light border-secondary mb-3" rows="5" required><%= chats.message %></textarea>
          <div class="text-center">
            <button class="btn btn-success px-4">
              <i class="bi bi-send-fill"></i> Update
            </button>
          </div>
        </div>
      </div>
    </form>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
